import React from 'react'

import './styles.css'

const ErrorFormGroup = ({ children }) => {
  return (
    <div className='error_form_group'>
      { children }
    </div>
  )
}

export default ErrorFormGroup